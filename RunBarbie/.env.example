# API URL when using real backend. Leave empty to use defaults (localhost / Android emulator 10.0.2.2).
# For physical device: set to your computer's IP. Run "npm run get-ip" in RunBarbie to get it.
# Example: EXPO_PUBLIC_API_URL=http://192.168.1.100:3000/api
EXPO_PUBLIC_API_URL=http://10.25.181.172:3000/api

# Optional: Facebook access token to fetch trail run events in Search > Highlight.
# Event search requires a USER access token (not app-only). Get one from:
# https://developers.facebook.com/tools/explorer/ (select your app, add permission, generate token)
# Or implement Facebook Login in the app and use the user's token.
# If unset, the app shows mock trail run events instead.
EXPO_PUBLIC_FACEBOOK_ACCESS_TOKEN=

# Optional: Google sign-in (Login screen). Get from Google Cloud Console.
# Create OAuth 2.0 Web client, add redirect URI (e.g. https://auth.expo.io/@your-username/run-barbie). See SOCIAL_LOGIN_SETUP.md.
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=
