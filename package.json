{
  "name": "run-barbie-project",
  "private": true,
  "description": "Run Barbie app + server monorepo",
  "scripts": {
    "start": "cd RunBarbie && npm start",
    "start:server": "cd server && node index.js"
  }
}
